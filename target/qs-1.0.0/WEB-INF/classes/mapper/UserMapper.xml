<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wjianwu.qs.dao.UserDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wjianwu.qs.entity.User">
        <id column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="account" property="account"/>
        <result column="user_name" property="userName"/>
        <result column="password" property="password"/>
        <result column="password_salt" property="passwordSalt"/>
        <result column="user_sex" property="userSex"/>
        <result column="user_face" property="userFace"/>
        <result column="user_integral" property="userIntegral"/>
        <result column="user_level" property="userLevel"/>
        <result column="user_status" property="userStatus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByAccount" parameterType="java.lang.String" resultType="com.wjianwu.qs.entity.User">
        SELECT
            *
        FROM
            qs_user
        WHERE
            account = #{account}
    </select>

    <select id="manyAccount" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT
            COUNT(*)
        FROM
            qs_user
        WHERE
            account = #{account}
    </select>

</mapper>
